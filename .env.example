# Database
# Prisma: use Neon pooler URL in production (…-pooler.…/…?sslmode=require)
DATABASE_URL="postgresql://postgres:password@localhost:5432/companies_intelligence"

# pg-boss queue (optional). Prefer Neon *direct* host (no -pooler) in production.
# Falls back to DATABASE_URL when unset. Required separately on Vercel when Prisma uses the pooler.
# QUEUE_DATABASE_URL="postgresql://…@ep-….eu-central-1.aws.neon.tech/neondb?sslmode=require"

# Auth - JWT secret (must match NextAuth secret on FE)
JWT_SECRET="your-super-secret-jwt-key-here"

# Server
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Storage
# Local/Docker: ./storage
# Vercel: /tmp/storage (ephemeral; exports are rebuilt from DB on download)
STORAGE_BASE_PATH="./storage"

# Worker concurrency (Mac / Docker worker process only)
WORKER_CONCURRENCY=5

# Set to true only for single-process Docker deploys that embed the worker in the API.
# Leave unset on Vercel and when running `npm run start:worker` on a separate machine.
# ENABLE_EMBEDDED_WORKER=true

# Persona Search — Brave Search API (free tier: 1,000 queries/month)
# Get your key at: https://api.search.brave.com
# Needed on the *worker* host (Mac), not required on the Vercel API.
BRAVE_SEARCH_API_KEY=""

# Relevance filter — Groq API (free tier, llama-3.1-8b-instant)
# Get your key at: https://console.groq.com
# Optional: if unset, Brave results are returned without LLM filtering
# Needed on the *worker* host for crawl enrichment / personalization / campaign email.
GROQ_API_KEY=""

# Email — choose one provider:
#
# Option A: Brevo REST API (https://app.brevo.com)
#   BREVO_API_KEY=...
#   EMAIL_FROM="Companies Intelligence <noreply@yourapp.com>"
#
# Option B: SMTP (e.g. mail.edamame.digital)
#   EMAIL_HOST=mail.example.com
#   EMAIL_PORT=465
#   EMAIL_SECURE=true
#   EMAIL_USER=contact@example.com
#   EMAIL_PASS=...
#   EMAIL_FROM=contact@example.com
#
# Development (no provider configured):
#   - No emails are sent; verification URL is printed to the API console.
#   - Register response includes devVerificationUrl when NODE_ENV is not production.
#
APP_URL="http://localhost:3001"
BREVO_API_KEY=
# EMAIL_HOST=
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=
# EMAIL_PASS=
EMAIL_FROM="Companies Intelligence <noreply@yourapp.com>"
